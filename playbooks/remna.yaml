---
- name: Install Remnawave
  hosts: remnawave
  remote_user: "{{ default_user }}"
  become: true
  roles:
    - remnawave
    - caddy
  tags:
    - remnawave
    - remna

- name: Install Remnanode
  hosts: remnanode
  remote_user: "{{ default_user }}"
  become: true
  roles:
    - remnanode
  tags:
    - remnanode
    - remna
